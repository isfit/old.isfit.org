class SppArticle < ActiveRecord::Base

validates_presence_of :title, :message => "Title can't be blank"
validates_presence_of :ingress, :message => "Ingress can't be blank"
validates_presence_of :article, :message => "Article can't be blank"

end
