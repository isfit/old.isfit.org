<h1>Listing personal_contacts</h1>

<table>
  <tr>
		<!--Uses sort helper to sort importen info. Need solution on region -->
		<th><%= sort_header_tag('id', :title => 'Sort by contact ID')%></th>
    <th><%= sort_header_tag('firstname', :text=> 'Firstname')%></th>
    <th>Lastname</th>
    <th width=80>Address</th>
    <th>Zip code</th>
    <th>Email</th>
    <th><%= sort_header_tag('city', :text=> 'City')%></th>
    <th><%= sort_header_tag('country_id', :text =>'Country')%></th>
		<th>Region</th>
  </tr>
	<!--Just render the list partial -->
<%= render :partial => 'personal_contacts/list', :locals => {:personal_contacts=> @personal_contacts}%>

</table>

<br />

<%= link_to 'New personal_contact', new_personal_contact_url %>
