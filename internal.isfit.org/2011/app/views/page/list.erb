<h1>Pages</h1>
<p>Below is a list of all pages (except news) available for isfit.org. Press edit(<%=image_tag("icon/edit.png",
    :alt => "E", :title => "Edit", :border => 0)%>) to see the content for each link.</p>
<table cellspacing="0" cellpadding="4" style="border: 1px solid #ccc">
	<tr>
		<td width="200"><b><%= sort_header_tag('tag',
            :text => 'Belongs under...') %></b></td>
        <td width="200"><b><%= sort_header_tag('title_en',
            :text => 'English title') %></b></td>
        <td width="230"><b><%= sort_header_tag('title_no',
            :text => 'Norwegian title') %></b></td>
		<td></td>
		<td></td>
    </tr>
<% row = 1 %>
<% @pages.each do |page| %>
    <tr class="page_tr<%= row % 2 == 1 ? ' alternate' : '' %>">
		<td><%#= page.tag.split('-')[0].capitalize%>  
			<% if Sublink.find(:first, :conditions => {:page_id => page.id})%>
				<%= Tab.find(Sublink.find(:first, :conditions => {:page_id => page.id}).tab_id).name_en.capitalize%>
			<%else%>
			No link yet...
			<%end%>
		</td>
        <td><%= page.title_en != '' ? page.title_en : "<em>No English version</em>" %> </td>
        <td><%= page.title_no != '' ? page.title_no : "<em>No Norwegian version</em>" %></td>
        <td>
            <%= link_to(image_tag("icon/edit.png",
                :alt => "E", :title => "Edit", :border => 0),
                {:action => 'create', :id => page.id}) %>
        </td>
		<td>
            <%= link_to(image_tag("icon/trash.png",
				:alt => "Delete", :title => "Delete",:border => 0), 
				{:action => "delete", :id => page.id},
				:confirm => 'Are you sure you want to delete this page') %>
        </td>
    </tr>
<% row += 1 %>
<% end %>
</table>
<p><%= will_paginate @pages %></p>
