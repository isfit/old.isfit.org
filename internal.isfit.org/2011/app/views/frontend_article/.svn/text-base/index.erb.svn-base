<h1>Articles</h1>
<p>Below is a list of all articles available for isfit.org. <br />
Article on top will be listed as main article on isfit.org, and the 6 next will also be on the front page <br />
The rest of the articles will be listed under archive</p>
<table cellspacing="0" cellpadding="4" style="border: 1px solid #ccc">
	<tr>
        <td width="200"><b>English title</b></td>
        <td width="230"><b>Norwegian title</b></td>
        <td width="80"><b>Created</b></td>
        <td width="70"><b>Priority</b></td>
        <td width="60"><b>Visible</b></td>
        <td width="100"><b>Press release</b></td>
		<td></td>
		<td></td>
    </tr>
<% row = 1 %>
<% @articles.each do |article| %>
    <tr class="article_tr<%= row % 2 == 1 ? ' alternate' : '' %>">
        <td><%= article.title_en != '' ? article.title_en : "<em>No English version</em>" %> </td>
        <td><%= article.title_no != '' ? article.title_no : "<em>No Norwegian version</em>" %></td>
		<td><%= article.created_at.strftime("%d %b %Y") %></td>
		<td><%#= article.weight %> <%=link_to("up", :action=>"moveup", :id => article.id)%> <%=link_to("down", :action=>"movedown", :id => article.id)%></td>
		<td><%= article.list ? "Yes" : "No" %></td>
		<td><%= article.press_release == 1 ? :"Yes" : "No"%></td>
        <td>
            <%= link_to(image_tag("icon/edit.png",
                :alt => "E", :title => "Edit", :border => 0),
                edit_frontend_article_path(params[:tab], article)) %>
        </td>
		<td>
            <%= link_to(image_tag("icon/trash.png",
				:alt => "Delete", :title => "Delete",:border => 0), 
				{:action => "delete", :id=>article.id, :tab=>params[:tab]},
				:confirm => 'Are you sure you want to delete this article?') %>
        </td>
    </tr>
<% row += 1 %>
<% end %>
</table>

